apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fast-api-dep-ui.fullname" . }}-test-connection"
  labels:
    {{- include "fast-api-dep-ui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fast-api-dep-ui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
