apiVersion: v1
kind: Service
metadata:
  name: {{ include "fast-api-dep-ui.fullname" . }}
  labels:
    {{- include "fast-api-dep-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fast-api-dep-ui.selectorLabels" . | nindent 4 }}
